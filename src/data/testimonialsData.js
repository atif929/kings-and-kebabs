// Real customer testimonials go here once available (e.g. pulled from Google
// Reviews, or supplied directly by the client). Intentionally left empty —
// the Testimonials section will not render fabricated quotes.
//
// Shape once populated:
// { id: string, quote: string, author: string, rating: number (1-5) }
export const TESTIMONIALS = [];
